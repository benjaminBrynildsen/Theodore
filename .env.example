# Theodore Environment Variables
# Copy this to .env and fill in your keys

# Database
DATABASE_URL=postgresql://wolfgang@/theodore?host=/var/run/postgresql

# AI Providers (add your keys here)
ANTHROPIC_API_KEY=sk-ant-api03-...
OPENAI_API_KEY=sk-...

# Server
PORT=3001

# Stripe (when ready)
# STRIPE_SECRET_KEY=sk_live_...
# STRIPE_WEBHOOK_SECRET=whsec_...
# Optional hosted app URL for Stripe success/cancel/portal redirects
# APP_URL=https://app.theodore.ai
# Optional explicit redirect overrides
# STRIPE_CHECKOUT_SUCCESS_URL=https://app.theodore.ai/?billing=success&session_id={CHECKOUT_SESSION_ID}
# STRIPE_CHECKOUT_CANCEL_URL=https://app.theodore.ai/?billing=cancel
# STRIPE_PORTAL_RETURN_URL=https://app.theodore.ai/
# Optional: only enable in local dev for debugging reset flows
# ALLOW_DEV_RESET_TOKEN=true
# Optional: use pre-created Stripe prices instead of dynamic price_data
# STRIPE_PRICE_WRITER=price_...
# STRIPE_PRICE_AUTHOR=price_...
# STRIPE_PRICE_STUDIO=price_...
# Pricing controls (10x margin default)
# CREDIT_COST_PER_CREDIT_USD=0.0001
# BILLING_MARKUP_MULTIPLE=10

# Email (for magic link auth, when ready)
# SMTP_HOST=smtp.resend.com
# SMTP_PORT=465
# SMTP_USER=resend
# SMTP_PASS=re_...
# FROM_EMAIL=hello@theodore.app
